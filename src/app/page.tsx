import React from 'react';

// Add study cycle (With options, for example: Pomodoro, Self, etc.)
// Add header that extends when hovered
// Add persistent header when scroll down
// Add attached videos for each model in skills, and each paragraph in paragraphs
// Add relation hints after submitting solutions in verbal analogy

export default function Home() {
	return <div>hi</div>;
}
